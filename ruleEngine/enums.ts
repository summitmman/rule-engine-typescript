export enum ConditionType {
  Condition = 'condition',
  And = 'and',
  Or = 'or',
  Not = 'not',
  Default = 'default'
}

export enum ConditionName {
  isBetween = 'isBetween',
  isEqual = 'isEqual'
}